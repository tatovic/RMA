<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- tech.md sekcija 8.6 — NetworkMonitor proverava stanje veze pre pokusaja sinhronizacije (tiket 28). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- FR-081 — slikanje kamerom; deklarisano da se moze traziti u toku rada (dozvola se obradjuje na licu mesta). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".HomeInventoryApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.HomeInventory">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.HomeInventory">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.AuthenticationActivity"
            android:exported="false"
            android:theme="@style/Theme.HomeInventory" />

        <activity
            android:name=".ui.ItemDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.HomeInventory" />

        <activity
            android:name=".ui.AdminActivity"
            android:exported="false"
            android:theme="@style/Theme.HomeInventory" />

        <!-- FR-081 — deljenje internog fajla sa aplikacijom kamere dok se fotografija snima. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
